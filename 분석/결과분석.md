# 📊 결과 페이지 분석

**분석 일시:** 2025-12-02 20:52:57 (KST)  
**분석 대상:** `src/pages/Result.tsx` (399줄)  
**페이지 역할:** 시험 결과 표시 및 해설 제공

---

## 📋 주요 기능

### 1️⃣ 시험 통계

```typescript
const correctCount = answers.filter((ans, idx) => 
  ans === questions[idx].answer
).length;

const wrongCount = answers.filter((ans, idx) => 
  ans !== null && ans !== questions[idx].answer
).length;

const unansweredCount = answers.filter(ans => ans === null).length;

const score = Math.round((correctCount / questions.length) * 100);
const passed = score >= 60;
```

### 2️⃣ 합격/불합격 표시

```typescript
{mode === 'timedRandom' && (
  <div className={passed 
    ? 'bg-green-100 text-green-800' 
    : 'bg-red-100 text-red-800'
  }>
    <h2>{passed ? '🎉 합격' : '❌ 불합격'}</h2>
    <p>합격 기준: 60점</p>
  </div>
)}
```

### 3️⃣ 오답노트 모드

```typescript
{mode === 'wrong' && (
  <div className="bg-blue-50">
    <h2>📝 오답노트 복습 결과</h2>
    <p>재응시 문제 {answeredCount}문제 응시해서 {correctCount}문제 맞췄습니다</p>
    <p>정답률: {percentage}%</p>
  </div>
)}
```

---

## 📈 카테고리별 통계

```typescript
<CategoryStats questions={questions} answers={answers} />

// 출력:
전기이론: 15/20 (75%)
전기기기: 12/20 (60%)
전기설비: 18/20 (90%)
```

---

## 📝 문제 해설

```typescript
<QuestionReview
  question={question}
  questionNumber={index + 1}
  userAnswer={answers[index]}
/>

// 표시 내용:
- 문제 내용
- 사용자 답변 (빨간색 또는 초록색)
- 정답 (파란색 강조)
- 해설
- 출제기준
```

---

## 💡 핵심 특징

- ✅ 실시간 통계 계산
- ✅ 카테고리별 정답률
- ✅ 문제별 상세 해설
- ✅ 출제기준 표시

---

**분석 완료 일시:** 2025-12-02 20:52:57 (KST)
