# 🔐 전기기능사 CBT - 관리자 메뉴 상세 분석

**분석 일시:** 2025-12-02 20:49:24 (KST)  
**분석 대상:** `src/pages/Admin.tsx` (4,576줄)  
**프로젝트:** 전기기능사 CBT 시스템

---

## 📊 메뉴 구조 개요

관리자 페이지는 총 **4,576줄**의 방대한 코드로 구성되어 있으며, **9개 주요 탭**으로 나뉩니다:

```typescript
type ActiveTab = 
  | 'questions'         // 문제 관리
  | 'members'           // 회원 관리
  | 'sync'              // 동기화
  | 'statistics'        // 통계
  | 'config'            // 출제 설정
  | 'login-history'     // 로그인 기록
  | 'feedbacks'         // 제보 게시판
  | 'upload'            // 문제 업로드
  | 'student-records'   // 학생 학습 기록
```

---

## 1️⃣ 문제 관리 (questions)

### 주요 기능

#### ✅ CRUD 작업
- **문제 추가** (`handleAddQuestion`)
  - 필수 항목 검증 (question, option1)
  - 출제기준 자동 적용 옵션
  - Supabase 양방향 동기화
  
- **문제 수정** (`handleUpdateQuestion`)
  - 기존 데이터 로드
  - 실시간 미리보기
  - Supabase 동기화 상태 피드백
  
- **문제 삭제** (`handleDeleteQuestion`)
  - 개별 삭제
  - 일괄 삭제 (`handleDeleteSelected`)
  - LocalStorage + Supabase 양쪽에서 삭제

#### ✅ 필터링 & 검색

```typescript
// 카테고리 필터
selectedCategory: '전체' | '전기이론' | '전기기기' | '전기설비' | '기타'

// 가중치 필터
weightFilter: 'all' | '1' | '2' | '3' | ... | '10'

// 특수 필터
showMustIncludeOnly: boolean   // 반드시 포함 문제만
showMustExcludeOnly: boolean   // 반드시 불포함 문제만

// 검색 대상
- 문제 ID
- 문제 내용 (question)
- 선택지 (option1-4)
- 해설 (explanation)
- 카테고리
```

#### ✅ 출제기준 관리

**자동 적용 시스템:**
```typescript
// 1단계: 키워드 기반 자동 매칭
matchedStandard = matchStandardByKeywords(question);

// 2단계: 매칭 실패 시 랜덤 적용
if (!matchedStandard) {
  const standards = getStandardsByCategory(category);
  matchedStandard = standards[Math.floor(Math.random() * standards.length)];
}

// 3단계: 세부항목 자동 할당
matchedDetailItem = matchDetailItemByKeywords(question);
```

**수동 적용:**
- `handleRandomApplyStandard()`: 선택 문제에 랜덤 출제기준 적용
- `handleManualApplyStandard()`: 선택 문제에 특정 출제기준 적용

#### ✅ 가중치 시스템

```typescript
가중치 1  = 최고 빈도 (자주 출제) = 쉬운 문제
가중치 5  = 중간 빈도 = 보통 문제
가중치 10 = 최저 빈도 (거의 안나옴) = 어려운 문제
```

**일괄 변경:**
```typescript
handleBulkWeightChange(newWeight: number)
// - 선택된 문제들의 가중치를 일괄 변경
// - Supabase 동기화
// - 실패 건수 피드백
```

#### ✅ UI 기능

- **뷰 모드 전환**: 카드뷰 ↔ 테이블뷰
- **페이지네이션**: 100개씩 표시
- **체크박스 선택**:
  - 개별 선택
  - 전체 선택/해제
  - 선택 개수 실시간 표시
- **미리보기 모달**: LaTeX 수식 렌더링 지원

### 데이터 소스

```typescript
// 로드 우선순위
loadQuestions() {
  1. Supabase에서 모든 문제 가져오기 (fetchAllQuestions)
  2. ID 내림차순 정렬 (최신 문제가 위로)
  3. LocalStorage에 캐싱
  4. 실패 시 LocalStorage fallback
}
```

### 통계 표시

```typescript
questionStats = {
  전체: questions.length,
  전기이론: questions.filter(q => q.category === '전기이론').length,
  전기기기: questions.filter(q => q.category === '전기기기').length,
  전기설비: questions.filter(q => q.category === '전기설비').length,
  기타: questions.filter(q => !['전기이론', '전기기기', '전기설비'].includes(q.category)).length,
}
```

---

## 2️⃣ 회원 관리 (members)

### 데이터 구조

```typescript
interface Member {
  id: number;
  name: string;
  phone: string;
  address: string;
  memo: string;
}
```

### 주요 기능

- **회원 추가** (`handleAddMember`)
  - ID 자동 생성
  - Supabase 동기화 (`saveMemberToSupabase`)
  
- **회원 수정** (`handleUpdateMember`)
  - LocalStorage + Supabase 양방향 업데이트
  
- **회원 삭제** (`handleDeleteMember`)
  - 확인 팝업
  - LocalStorage + Supabase 양쪽에서 삭제

### 데이터 소스

```typescript
loadMembers() {
  const allMembers = getMembers(); // LocalStorage
  setMembers(allMembers);
}
```

---

## 3️⃣ 동기화 (sync)

### 🔄 3가지 동기화 방식

#### A. Google Sheets ↔ LocalStorage

**가져오기 (`handleSyncFromSheets`)**

```typescript
// 지원 시트 목록
const SHEET_NAMES = [
  'questions',    // 메인 시트
  '전기이론',      // 카테고리별 시트
  '전기기기',
  '전기설비',
  '기타'
];

// 시트별 선택 가능
selectedSheets: string[]

// 동기화 모드
syncMode: 'merge' | 'replace'
// - merge: 시트 데이터로 업데이트 + 로컬 전용 문제 유지
// - replace: 시트 데이터로 완전 교체
```

**특징:**
- 중복 ID 자동 조정 (1000-1999 범위)
- API 제한 방지 (0.5초 지연)
- 카테고리별 통계 자동 표시
- 상세한 진행 로그

**업로드 (`handleSyncToSheets`)**

```typescript
// LocalStorage → Google Sheets
bulkAddQuestionsToSheets(questions, selectedSheets)
// - 카테고리별 시트에 분산 저장
// - 배치 업로드 (2개씩)
// - 1초 간격 (API 안정성)
```

#### B. Supabase 마이그레이션

```typescript
// 연결 테스트
testSupabaseConnection() → boolean

// 문제 수 확인
getSupabaseQuestionCount() → number

// 마이그레이션
migrateQuestionsToSupabase({
  total: number,
  current: number,
  status: 'idle' | 'processing' | 'complete' | 'error',
  message: string
})

// 사용량 통계
getSupabaseUsageStats() → {
  dbSize: string,
  storageUsed: string,
  storageLimit: string,
  tableRowCounts: object
}
```

#### C. 데이터 백업/복구

```typescript
// 내보내기
exportData() → JSON 파일 다운로드

// 가져오기
importData(file: File) → {
  questions: Question[],
  members: Member[]
}

// 백업
downloadBackup() → questions-backup-YYYYMMDD-HHMMSS.json

// 복구
restoreFromFile(file: File)

// 초기화
deleteAllData() {
  1. 백업 자동 다운로드
  2. 모든 데이터 삭제
  3. 브라우저 캐시 삭제
  4. 서버에서 최신 데이터 로드
  5. 페이지 새로고침
}

// 캐시 삭제
clearAllCaches() {
  - LocalStorage
  - SessionStorage
  - Cache API
  - IndexedDB
}
```

---

## 4️⃣ 통계 (statistics)

### 📈 출제기준 분석

```typescript
<StandardStatistics questions={questions} />
```

**표시 내용:**
- 카테고리별 출제기준 분포
- 각 출제기준별 문제 수
- 시각화 차트
- 세부항목 통계

---

## 5️⃣ 출제 설정 (config)

### ⚙️ 시험 구성

```typescript
interface ExamConfig {
  // 문제 수 설정
  questionCount: {
    전기이론: number,  // 기본 20
    전기기기: number,  // 기본 20
    전기설비: number   // 기본 20
  },
  
  // 가중치별 분포
  weightDistribution: {
    [weight: 1-10]: number
  },
  
  // 필수 포함/제외
  mustInclude: boolean,
  mustExclude: boolean,
  
  // 시험 시간
  timeLimit: number  // 분 단위
}
```

### 실시간 통계

```typescript
// 전체 가중치별 문제 개수
weightCounts: { [weight: number]: number }

// 카테고리별 가중치 개수
categoryCounts: {
  전기이론: { [weight: number]: number },
  전기기기: { [weight: number]: number },
  전기설비: { [weight: number]: number }
}
```

**예시:**
```
📊 가중치별 문제 개수:
   가중치 1: 150개 (최고빈도)
   가중치 5: 300개 (중간)
   가중치 10: 50개 (최저빈도)

📊 카테고리별 가중치 개수:
   전기이론:
     - 가중치 1: 50개
     - 가중치 5: 100개
     - 가중치 10: 20개
```

### 함수

```typescript
// 설정 저장
handleSaveExamConfig()

// 설정 초기화
handleResetExamConfig()

// 가중치 선택/해제
toggleWeight(weight: number)
```

---

## 6️⃣ 로그인 기록 (login-history)

### 데이터 구조

```typescript
interface LoginHistory {
  id: number;
  userId: number;
  userName: string;
  timestamp: number;
  device?: string;      // 'PC' | '스마트폰' | '태블릿'
  userAgent?: string;
}
```

### 기기 감지 로직

```typescript
const getDeviceType = (userAgent?: string): string => {
  const ua = userAgent.toLowerCase();
  
  // 태블릿 체크 (우선)
  if (ua.includes('ipad') || 
      (ua.includes('tablet') && !ua.includes('mobile')) ||
      (ua.includes('android') && !ua.includes('mobile'))) {
    return '태블릿';
  }
  
  // 스마트폰 체크
  if (ua.includes('mobile') || 
      ua.includes('iphone') || 
      ua.includes('ipod') ||
      (ua.includes('android') && ua.includes('mobile'))) {
    return '스마트폰';
  }
  
  // PC
  return 'PC';
}
```

### 로드 방식

```typescript
loadLoginHistory() {
  1. Supabase에서 로그인 기록 불러오기
  2. LocalStorage에서도 로그인 기록 불러오기
  3. 두 데이터 병합 (중복 제거)
     - Supabase 기록 우선
     - LocalStorage에만 있는 기록 추가 (모바일 기록 누락 방지)
  4. 최신순 정렬 (timestamp 역순)
  5. state 업데이트
}
```

**병합 로직:**
```typescript
const mergedHistory = [...supabaseHistory];
const supabaseIds = new Set(supabaseHistory.map(h => h.id));

// LocalStorage에만 있는 기록 추가
for (const local of localHistory) {
  if (!supabaseIds.has(local.id)) {
    mergedHistory.push(local);
  }
}

// 최신순 정렬
mergedHistory.sort((a, b) => b.timestamp - a.timestamp);
```

### 주요 기능

- ✅ 실시간 로딩 상태 (`isLoadingLoginHistory`)
- ✅ 개별 삭제 (`deleteLoginHistory`)
- ✅ 전체 삭제 (`clearLoginHistory`)
- ✅ 새로고침 버튼

---

## 7️⃣ 제보 게시판 (feedbacks)

### 하위 탭 구조

```typescript
feedbackSubTab: 'bug' | 'suggestion' | 'question'
```

### 커스텀 훅 사용

```typescript
const {
  feedbacks,              // 현재 필터된 피드백 목록
  allFeedbacksCount,      // 전체 피드백 개수
  loading,                // 로딩 상태
  error,                  // 에러 상태
  loadFeedbacks,          // 피드백 로드 함수
  deleteFeedbackItem      // 피드백 삭제 함수
} = useFeedbacks({
  isAdmin: true,
  filterType: feedbackSubTab
});
```

### 데이터 구조 (추정)

```typescript
interface Feedback {
  id: number;
  type: 'bug' | 'suggestion' | 'question';
  title: string;
  description: string;
  userId: number;
  userName?: string;
  timestamp: number;
  status?: 'pending' | 'resolved';
}
```

### 주요 기능

- ✅ 타입별 필터링 (버그/건의/문의)
- ✅ 삭제 기능
- ✅ 로딩/에러 상태 표시
- ✅ 전체 개수 표시

---

## 8️⃣ 문제 업로드 (upload)

### 2가지 업로드 방식

#### A. Google Sheets URL

```typescript
uploadMethod = 'googleSheet'

// 프로세스
1. Google Sheets 공개 URL 입력
   - 예: https://docs.google.com/spreadsheets/d/...
   
2. "가져오기" 버튼 클릭
   - fetchQuestionsFromGoogleSheet(googleSheetUrl)
   
3. 자동 파싱 및 미리보기
   - 처음 5개 문제 표시
   - 카테고리별 통계
   
4. DB 저장
   - insertQuestions(uploadPreview)
   - 성공/실패 개수 표시
```

#### B. CSV 파일 업로드

```typescript
uploadMethod = 'csv'

// 프로세스
1. CSV 파일 선택 (.csv)
   - input type="file" accept=".csv"
   
2. "파싱하기" 버튼 클릭
   - const text = await csvFile.text()
   - const questions = parseCSVToQuestions(text)
   
3. 미리보기 및 통계
   - 처음 5개 문제
   - 전기이론/전기기기/전기설비 개수
   
4. DB 저장
   - insertQuestions(uploadPreview)
```

### UI 컴포넌트

```typescript
// 업로드 방식 선택
<select value={uploadMethod} onChange={...}>
  <option value="googleSheet">Google Sheets URL</option>
  <option value="csv">CSV 파일</option>
</select>

// 상태 메시지
{uploadStatus && (
  <div className={
    uploadStatus.includes('✅') ? 'bg-green-50 text-green-800' :
    uploadStatus.includes('❌') ? 'bg-red-50 text-red-800' :
    'bg-blue-50 text-blue-800'
  }>
    {uploadStatus}
  </div>
)}

// 미리보기
{uploadPreview.length > 0 && (
  <>
    <h3>미리보기 (처음 5개)</h3>
    {uploadPreview.slice(0, 5).map(...)}
    
    {/* 카테고리별 통계 */}
    <div className="grid grid-cols-3 gap-4">
      <div>전기이론: X개</div>
      <div>전기기기: Y개</div>
      <div>전기설비: Z개</div>
    </div>
    
    {/* 저장 버튼 */}
    <button onClick={async () => {
      const result = await insertQuestions(uploadPreview);
      // 성공: result.success개, 실패: result.failed개
    }}>
      📥 {uploadPreview.length}개 문제를 DB에 저장
    </button>
  </>
)}
```

### 결과 처리

```typescript
const result = await insertQuestions(uploadPreview);

setUploadStatus(
  `✅ 완료! 성공: ${result.success}개, 실패: ${result.failed}개`
);

if (result.errors.length > 0) {
  console.error('업로드 오류:', result.errors);
}

// 성공 시 문제 수 자동 갱신
if (result.success > 0) {
  const newCount = await getSupabaseQuestionCount();
  setSupabaseQuestionCount(newCount);
}
```

---

## 9️⃣ 학생 학습 기록 (student-records)

### 데이터 구조

```typescript
interface StudentRecord {
  userId: number;
  userName: string;
  wrongAnswers: unknown[];     // 오답노트
  examResults: unknown[];      // 시험 결과 기록
  statistics: {
    averageScore?: number;     // 평균 점수
    // ... 기타 통계 정보
  } | null;
  updatedAt: string;
}
```

### 로드 방식

```typescript
loadStudentRecords() {
  1. Supabase에서 모든 사용자 데이터 가져오기
     - fetchAllUserDataFromSupabase()
     
  2. 회원 정보와 매칭
     - members.find(m => m.id === userData.userId)
     - 매칭 실패 시: "회원 #123" 형식으로 표시
     
  3. state 업데이트
}
```

### UI 표시 (카드 형식)

```typescript
<div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
  {studentRecords.map(record => (
    <div 
      className={selectedStudentId === record.userId 
        ? 'border-blue-500 bg-blue-50'  // 선택됨
        : 'border-gray-200'               // 기본
      }
      onClick={() => setSelectedStudentId(...)}
    >
      <h3>{record.userName}</h3>
      <span>ID: {record.userId}</span>
      
      <p>📝 시험 횟수: {record.examResults.length}회</p>
      <p>❌ 오답 문제: {record.wrongAnswers.length}개</p>
      <p>📊 평균 점수: {record.statistics.averageScore}점</p>
      
      <p>최종 업데이트: {record.updatedAt}</p>
    </div>
  ))}
</div>
```

### 주요 기능

- ✅ 카드 형식 디스플레이 (그리드 레이아웃)
- ✅ 클릭하여 상세 정보 확장/축소
- ✅ 로딩 상태 표시
- ✅ 데이터 없을 때 안내 메시지
- ✅ 새로고침 버튼

---

## 🔐 보안 및 인증

### 현재 구현

```typescript
const ADMIN_PASSWORD = 'admin2024';  // ⚠️ 하드코딩됨

const handleLogin = () => {
  if (password === ADMIN_PASSWORD) {
    setIsAuthenticated(true);
  } else {
    alert('비밀번호가 틀렸습니다.');
  }
};
```

### ⚠️ 보안 이슈

1. **하드코딩된 비밀번호**: 소스 코드에 평문으로 노출
2. **세션 관리 없음**: 새로고침 시 재로그인 필요
3. **권한 체크 없음**: 로그인만 하면 모든 기능 접근 가능
4. **암호화 없음**: 비밀번호 전송 시 암호화 없음

### 💡 개선 권장사항

```typescript
// 1. 환경 변수로 관리
const ADMIN_PASSWORD = import.meta.env.VITE_ADMIN_PASSWORD;

// 2. 세션 저장
localStorage.setItem('adminAuth', 'true');

// 3. JWT 토큰 사용
const token = await loginAPI(username, password);

// 4. Supabase Auth 사용
const { data, error } = await supabase.auth.signInWithPassword({
  email: 'admin@example.com',
  password: password
});
```

---

## 📦 데이터 흐름 아키텍처

### 3중 저장소 구조

```
┌─────────────────────────────────────────────────────────────┐
│                        관리자 페이지                          │
└─────────────────────────────────────────────────────────────┘
                              │
                              ▼
        ┌─────────────────────────────────────────┐
        │         데이터 동기화 레이어              │
        └─────────────────────────────────────────┘
                    │         │         │
        ┌───────────┴─────┬───┴────┬────┴──────┐
        ▼                 ▼        ▼           ▼
    ┌────────┐     ┌─────────┐  ┌──────┐  ┌────────┐
    │Supabase│ ←→  │LocalS-  │  │Google│  │학생 앱  │
    │  (DB)  │     │ torage  │  │Sheets│  │       │
    └────────┘     └─────────┘  └──────┘  └────────┘
        ↑              ↑           ↑           ↓
        │              │           │      ┌────────┐
        │              └───────────┴──────│시험응시 │
        │                                 └────────┘
        │                                      ↓
        │                                 ┌────────┐
        └─────────────────────────────────│결과저장 │
                                          └────────┘
```

### 동기화 방향

```typescript
// 1. 문제 데이터 가져오기 (로드)
Google Sheets → LocalStorage → Supabase → 학생 앱

// 2. 문제 데이터 저장 (동기화)
학생 앱/관리자 → Supabase → LocalStorage → Google Sheets

// 3. 학습 기록
학생 앱 → Supabase → 관리자
```

### Fallback 체계

```typescript
loadQuestions() {
  try {
    // 1순위: Supabase
    const questions = await fetchAllQuestions();
    if (questions && questions.length > 0) {
      saveQuestions(questions);  // LocalStorage 캐싱
      return questions;
    }
  } catch (error) {
    // 2순위: LocalStorage fallback
    return getQuestions();
  }
}
```

---

## 🎨 UI/UX 특징

### 디자인 시스템

#### 색상 팔레트

```typescript
// 카테고리별 색상
전기이론: {
  primary: 'blue-500',      // 파란색
  light: 'blue-50',
  text: 'blue-800'
}

전기기기: {
  primary: 'purple-500',    // 보라색
  light: 'purple-50',
  text: 'purple-800'
}

전기설비: {
  primary: 'orange-500',    // 주황색
  light: 'orange-50',
  text: 'orange-800'
}
```

#### 상태별 색상

```typescript
// 성공
success: 'bg-green-50 text-green-800'

// 오류
error: 'bg-red-50 text-red-800'

// 정보
info: 'bg-blue-50 text-blue-800'

// 경고
warning: 'bg-yellow-50 text-yellow-800'
```

### 모달 시스템

```typescript
// 6가지 모달
showAddModal: boolean          // 문제 추가
showEditModal: boolean         // 문제 수정
showPreviewModal: boolean      // 문제 미리보기
showStandardApplyModal: boolean // 출제기준 적용
showAddMemberModal: boolean     // 회원 추가
showEditMemberModal: boolean    // 회원 수정
```

### 반응형 레이아웃

```typescript
// 그리드 시스템
className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4"
// - 모바일: 1열
// - 태블릿: 2열
// - PC: 3열

// 카드 크기
className="p-4 rounded-lg border-2 cursor-pointer transition-all"
```

### 체크박스 인터랙션

```typescript
// 개별 선택
handleCheckboxChange(id: number, checked: boolean)

// 전체 선택
handleSelectAll(checked: boolean)

// 선택 상태 표시
{selectedQuestions.size > 0 && (
  <div>선택된 문제: {selectedQuestions.size}개</div>
)}
```

### 로딩 상태 표시

```typescript
// 패턴 1: 버튼 비활성화
<button disabled={isLoading}>
  {isLoading ? '처리 중...' : '저장'}
</button>

// 패턴 2: 전체 화면 로딩
{isLoadingLoginHistory ? (
  <div className="text-center py-12">
    <p>로딩 중...</p>
  </div>
) : (
  // 실제 컨텐츠
)}

// 패턴 3: 상태별 메시지
{syncLoading && <div>동기화 중...</div>}
{syncMessage && <div>{syncMessage}</div>}
```

---

## ⚙️ 주요 함수 및 로직

### 문제 관리 함수

```typescript
// 문제 로드
loadQuestions() → Promise<void>
// - Supabase 우선
// - LocalStorage fallback
// - ID 내림차순 정렬

// 문제 추가
handleAddQuestion() → void
// - 필수 항목 검증
// - 출제기준 자동 적용
// - Supabase 동기화

// 문제 수정
handleUpdateQuestion() → Promise<void>
// - 로컬 업데이트
// - Supabase 동기화
// - 성공/실패 피드백

// 문제 삭제
handleDeleteQuestion(id: number) → Promise<void>
handleDeleteSelected() → Promise<void>
// - 확인 팝업
// - 일괄 삭제 지원
// - Supabase 동기화
```

### 출제기준 함수

```typescript
// 랜덤 적용
handleRandomApplyStandard() → void
// - 선택된 문제에 랜덤 출제기준 적용
// - 카테고리별 출제기준 목록 사용
// - 세부항목 자동 할당

// 수동 적용
handleManualApplyStandard() → void
// - 특정 출제기준 선택
// - 카테고리 일치 검증
// - 세부항목 옵션

// 전체 일괄 적용
handleApplyStandardsToAll() → void
// - 모든 문제에 출제기준 적용
// - 키워드 매칭 우선
// - 랜덤 매칭 fallback
```

### 가중치 함수

```typescript
// 일괄 변경
handleBulkWeightChange(newWeight: number) → Promise<void>
// - 선택된 문제의 가중치 변경
// - Supabase 병렬 업데이트
// - 실패 건수 추적

// 토글 선택
toggleWeight(weight: number) → void
// - 가중치 필터 선택/해제
```

### 동기화 함수

```typescript
// Google Sheets → LocalStorage
handleSyncFromSheets() → Promise<void>
// - 시트별 선택 가능
// - 중복 ID 자동 조정
// - 병합/교체 모드
// - 출제기준 자동 적용 옵션

// LocalStorage → Google Sheets
handleSyncToSheets() → Promise<void>
// - 카테고리별 시트 분산
// - 배치 업로드 (2개씩)
// - 1초 간격

// 시트 선택
toggleSheetSelection(sheetName: string) → void
toggleAllSheets(checked: boolean) → void
```

### 로그인 기록 함수

```typescript
// 로드
loadLoginHistory() → Promise<void>
// - Supabase + LocalStorage 병합
// - 중복 제거
// - 최신순 정렬

// 기기 타입 감지
getDeviceType(userAgent?: string) → string
// - 태블릿 우선 체크
// - 스마트폰 체크
// - PC 기본값
```

### 학생 기록 함수

```typescript
// 로드
loadStudentRecords() → Promise<void>
// - Supabase에서 모든 사용자 데이터
// - 회원 정보와 매칭
// - userName 자동 설정
```

---

## 📊 성능 최적화

### 페이지네이션

```typescript
const questionsPerPage = 100;
const totalPages = Math.ceil(filteredQuestions.length / questionsPerPage);
const startIndex = (currentPage - 1) * questionsPerPage;
const endIndex = startIndex + questionsPerPage;
const currentQuestions = filteredQuestions.slice(startIndex, endIndex);
```

**효과:**
- 한 번에 100개만 렌더링
- 대용량 데이터 처리 가능
- UI 반응 속도 향상

### 시트별 선택 동기화

```typescript
// 전체 시트 동기화: 약 30-40초
// 단일 시트 동기화: 약 5-10초 (4배 빠름)

selectedSheets: string[] = ['전기이론']  // 필요한 시트만 선택
```

### API 호출 최적화

```typescript
// 배치 업로드
bulkAddQuestionsToSheets(questions, selectedSheets)
// - 2개씩 배치
// - 1초 간격 (API 제한 회피)

// 지연 추가
await new Promise(resolve => setTimeout(resolve, 1000));
```

### LocalStorage 캐싱

```typescript
// 패턴
1. Supabase에서 데이터 로드
2. LocalStorage에 캐싱
3. 다음 로드 시 LocalStorage 우선

// 장점
- 오프라인 지원
- 로딩 속도 향상
- Supabase 실패 시 fallback
```

---

## 🔧 개선 가능 영역

### 1. 보안

**현재 문제:**
- 하드코딩된 비밀번호
- 세션 관리 없음
- 권한 체크 없음

**개선안:**
```typescript
// Supabase Auth 사용
const { data, error } = await supabase.auth.signInWithPassword({
  email: 'admin@example.com',
  password: password
});

// 세션 저장
const session = await supabase.auth.getSession();

// 권한 기반 접근 제어 (RBAC)
if (user.role !== 'admin') {
  throw new Error('권한 없음');
}
```

### 2. 코드 구조

**현재 문제:**
- 4,576줄 단일 파일 (거대한 컴포넌트)
- 관심사 분리 부족
- 유지보수 어려움

**개선안:**
```typescript
// 탭별 컴포넌트 분리
src/pages/admin/
  ├── QuestionManagement.tsx
  ├── MemberManagement.tsx
  ├── SyncPanel.tsx
  ├── Statistics.tsx
  ├── ExamConfig.tsx
  ├── LoginHistory.tsx
  ├── Feedbacks.tsx
  ├── Upload.tsx
  └── StudentRecords.tsx

// 공통 훅
src/hooks/
  ├── useQuestions.ts
  ├── useMembers.ts
  ├── useSync.ts
  └── useLoginHistory.ts
```

### 3. 타입 안전성

**현재 문제:**
```typescript
wrongAnswers: unknown[]
examResults: unknown[]
statistics: unknown
```

**개선안:**
```typescript
interface WrongAnswer {
  questionId: number;
  userAnswer: number;
  correctAnswer: number;
  timestamp: number;
}

interface ExamResult {
  examId: number;
  score: number;
  correctCount: number;
  totalCount: number;
  timestamp: number;
}

interface Statistics {
  averageScore: number;
  totalExams: number;
  passCount: number;
  failCount: number;
}
```

### 4. 에러 처리

**현재:**
```typescript
try {
  // 작업
} catch (error) {
  console.error(error);
}
```

**개선안:**
```typescript
try {
  // 작업
} catch (error) {
  // 에러 로깅 서비스 (Sentry 등)
  logError(error);
  
  // 사용자 친화적 메시지
  showToast({
    type: 'error',
    message: '문제 로드에 실패했습니다. 다시 시도해주세요.',
    action: () => loadQuestions()
  });
}
```

### 5. 성능

**현재:**
```typescript
// 모든 문제를 한 번에 로드
const allQuestions = await fetchAllQuestions();
```

**개선안:**
```typescript
// 무한 스크롤 또는 가상 스크롤
const { data, hasMore, loadMore } = useInfiniteScroll({
  fetchFn: fetchQuestions,
  pageSize: 100
});

// React Virtual
import { useVirtualizer } from '@tanstack/react-virtual';
```

---

## 📝 요약

### 핵심 기능

1. **문제 은행 관리**: CRUD, 필터링, 검색, 출제기준, 가중치
2. **회원 관리**: 회원 정보 CRUD
3. **데이터 동기화**: Google Sheets ↔ LocalStorage ↔ Supabase
4. **통계 분석**: 출제기준, 학습 기록
5. **시험 설정**: 문제 수, 가중치 분포
6. **모니터링**: 로그인 기록, 제보, 학습 기록
7. **문제 업로드**: Google Sheets URL, CSV 파일

### 기술 스택

- **프론트엔드**: React 19 + TypeScript
- **스타일링**: TailwindCSS
- **백엔드**: Supabase (PostgreSQL)
- **동기화**: Google Sheets API
- **캐싱**: LocalStorage

### 데이터 흐름

```
Supabase (메인) ↔ LocalStorage (캐시) ↔ Google Sheets (백업)
```

### 특징

- ✅ 3중 데이터 저장소 (안정성)
- ✅ 스마트 출제기준 시스템 (자동 매칭)
- ✅ 가중치 기반 난이도 조절
- ✅ 실시간 통계 및 모니터링
- ✅ 에러 처리 및 fallback 체계
- ✅ 반응형 UI

---

**분석 완료 일시:** 2025-12-02 20:49:24 (KST)
