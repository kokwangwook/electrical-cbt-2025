# 📝 회원가입 페이지 분석

**분석 일시:** 2025-12-02 20:52:57 (KST)  
**분석 대상:** `src/pages/Register.tsx` (271줄)  
**페이지 역할:** 신규 회원 등록

---

## 📋 주요 기능

### 1️⃣ 입력 폼

```typescript
const [formData, setFormData] = useState({
  name: '',        // 필수
  phone: '',       // 필수
  email: '',       // 선택
  address: '',     // 선택
  memo: '',        // 선택
});
```

### 2️⃣ 유효성 검사

```typescript
validateForm(): string | null {
  // 이름 검사
  if (!formData.name.trim()) {
    return '이름을 입력하세요.';
  }
  if (formData.name.length < 2) {
    return '이름은 2자 이상이어야 합니다.';
  }
  
  // 전화번호 검사
  if (!formData.phone.trim()) {
    return '전화번호를 입력하세요.';
  }
  const phoneRegex = /^010-?\d{4}-?\d{4}$/;
  if (!phoneRegex.test(formData.phone)) {
    return '전화번호 형식이 올바르지 않습니다. (예: 010-1234-5678)';
  }
  
  // 이메일 검사 (선택)
  if (formData.email.trim()) {
    const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
    if (!emailRegex.test(formData.email)) {
      return '이메일 형식이 올바르지 않습니다.';
    }
  }
  
  // 중복 검사
  const existing = getMemberByName(formData.name);
  if (existing) {
    return `이미 등록된 회원입니다: ${formData.name}`;
  }
  
  return null;
}
```

### 3️⃣ 회원 등록

```typescript
handleSubmit() {
  const error = validateForm();
  if (error) {
    alert(error);
    return;
  }
  
  // LocalStorage에 저장
  const newMember = addMember({
    name: formData.name,
    phone: formData.phone,
    email: formData.email,
    address: formData.address,
    memo: formData.memo,
  });
  
  // Supabase에 저장
  await saveMemberToSupabase(newMember);
  
  alert('회원가입이 완료되었습니다!');
  onRegisterSuccess();
}
```

---

## 🎨 UI/UX

### 입력 필드

```typescript
<input
  name="name"
  value={formData.name}
  onChange={handleInputChange}
  placeholder="홍길동"
  required
  className="px-4 py-3 border rounded-lg"
/>
```

### 필수/선택 표시

```typescript
<label>
  이름 <span className="text-red-500">*</span>
</label>
<label>
  이메일 <span className="text-gray-400">(선택)</span>
</label>
```

---

## 💡 핵심 특징

- ✅ 실시간 유효성 검사
- ✅ 전화번호 형식 검증
- ✅ 이메일 형식 검증
- ✅ 중복 회원 검사
- ✅ Supabase 자동 동기화

---

**분석 완료 일시:** 2025-12-02 20:52:57 (KST)
